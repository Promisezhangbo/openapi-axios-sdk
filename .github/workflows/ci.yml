name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        node-version: ['20', '22']
    uses: ./.github/workflows/reusable-check.yml
    with:
      node-version: ${{ matrix.node-version }}
